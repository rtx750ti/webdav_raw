//! 工作流测试入口：只登记子模块，不放用例。
//!
//! 这一套测试**只针对真实 WebDAV 服务器**，不使用任何 mock/fake 服务器。
//! 它按一条完整业务链路依次使用本库的全部能力，并在每一步用服务端自己的记录核对
//! 结果，因此能回答「这些功能在真实服务器上到底管不管用」。
//!
//! 网络模块只在启用 `network-test` 时参与编译；其中的用例全部带 `#[ignore]`，
//! 避免在无人看守时对真实服务器发起写操作。

mod common;
mod support;

#[cfg(feature = "network-test")]
mod network;
