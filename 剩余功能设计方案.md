# 剩余 WebDAV 功能设计方案

本文档是 `webdav-core` 剩余 6 个 WebDAV 领域的实现准绳。实施期间以本文件为准，任何偏离都要先改本文件、再改代码。

---

## 一、目标

在已实现的认证、GET、PROPFIND、PUT 四个领域之上，补齐 6 个 WebDAV 方法领域：

| 领域 | HTTP 方法 | 一句话职责 |
|---|---|---|
| `delete` | DELETE | 删除资源，支持递归删集合 |
| `head` | HEAD | 只取元数据，不取响应体 |
| `mkcol` | MKCOL | 创建集合（目录） |
| `options` | OPTIONS | 探测服务端能力（DAV 级别、允许方法） |
| `copy` | COPY | 复制资源到新位置 |
| `r#move` | MOVE | 移动/重命名资源 |

> 注：`r#move` 是实现用的模块名（`move` 是 Rust 关键字，模块只能写成原始标识符）。**对外入口名是 `mv()`**，调用方写 `auth.mv()`，看不到 `r#`。

**目标效果**：调用方从已建好的 `WebdavAuth` 出发，用同一套手感写出完整的工作流——探测能力、建目录、查元数据、复制、移动、删除——路径错误走 `Result` 不 panic，HTTP 状态码一律原样透传，库不替调用方判定业务成败。完整用法见[第九章](#九目标效果完整用法)。

---

## 二、边界

### 2.1 绝对不动的部分

已实现的四个领域**一个字都不改**：

```text
src/auth.rs          ← 例外：只允许在文件末尾「追加」工厂方法，既有内容不改
src/get.rs
src/get/builder.rs
src/propfind.rs
src/propfind/**      ← 含 Depth、FindProp、MultiStatus 等全部既有类型
src/put.rs
src/put/**
```

对应的既有测试也一律不碰：

```text
tests/auth/**  tests/get/**  tests/propfind/**  tests/put/**
```

`lib.rs` 里这四个领域的 `mod` 声明与全部既有再导出，同样一行不动。

### 2.2 PROPPATCH 不在本方案范围内

PROPPATCH 领域由项目所有者自行实现，本方案**不设计、不实现、不测试、不示例**：

- `src/proppatch.rs`：保持现状（`pub mod builder;`），不动
- `src/proppatch/builder.rs`：保持 0 字节空文件，不动
- `src/proppatch/` 下不创建任何新文件
- `lib.rs` 里的 `pub mod proppatch;`：保持 `pub mod` 不变，**不改成私有**
- 不加 PROPPATCH 的 `pub use`，不加 `proppatch()` 工厂方法，不加 Cargo 测试目标

这样做的原因是：保持 `pub mod proppatch;` 后，实现者可以直接往 `src/proppatch/builder.rs` 里写代码并在 `lib.rs` 之外正常引用，不需要因为本方案而改动 `lib.rs`。

### 2.3 `src/proppath.rs`

0 字节遗留空文件，**保持原样**，不删除、不复用其名字。

### 2.4 非目标（本库不做的事）

对齐 PUT 领域的既有设计边界：

- 不做锁（LOCK / UNLOCK）
- 不做自动重试、断点续传、批量任务编排
- 不做递归建目录（MKCOL 不递归，需要多层就调用方自己循环）
- 不把 HTTP 状态码翻译成业务成功/失败：200/201/204/207/4xx/5xx 全部原样交给调用方
- 不解析响应体做业务判断（`copy` 的 207 反序列化是可选入口，不是自动判定）

---

## 三、设计约定

### 3.1 每领域完全自包含，不跨领域复用

这是本方案最重要的一条约定，也是与「已实现领域零改动」这条硬约束配套的必然结果。

| 会被复用的东西 | 本方案的处理 | 原因 |
|---|---|---|
| 路径解析（拼 base、解析绝对 URL） | 6 个领域各写各的，约 6 行 | 共享层要新建 `src/path.rs` 这类跨领域模块，属于结构性改动，不做 |
| `Depth` 请求头 | 各领域定义自己的受限枚举 | 复用 `propfind::Depth` 需要给 `propfind.rs` 加 `pub`，那是对已实现领域的改动 |
| `Overwrite` 请求头 | COPY 定义，MOVE 只读引用 | MOVE 语义就是「COPY + 删源」，共用同一协议值类型是对的；且是只读引用，不改 COPY 文件 |
| `EmptyElement`（`<D:collection/>` 占位） | 各领域不复用 | 同上，避免动 `propfind::raw_xml` |
| 手工 XML 写出（`quick_xml::Writer` 装配） | 各领域各写各的，约 10 行 setup | 与 `find_props.rs` 同款写法，但不抽公共模块 |

**跨领域允许的唯一引用**：`move` 引用 `crate::copy::builder::Overwrite`。

### 3.2 `Depth` 按领域裁剪，不做全开放枚举

`propfind::Depth` 是 `{Zero, One, Infinity}` 全开放枚举，因为 PROPFIND 三种都合法。但：

- **DELETE** 只允许 `0` 和 `infinity`（RFC 4918 §9.6.1），`Depth: 1` 是协议非法
- **COPY / MOVE** 三种都允许

因此本方案给 DELETE 定义两态枚举。类型系统层面就让 `Depth::One` 无法出现在 DELETE 上——这正是 PUT 撤回复盘中「构造点自证、不要在类型上留后门」那条边界的应用。

### 3.3 路径 API：统一用 `target_path` / `target_url` / `source_path` / `source_url`

```rust
// 单地址领域（DELETE / HEAD / MKCOL / OPTIONS）：只有一个要操作的地址
pub fn target_path(self, path: &str) -> Result<Self, XxxError>   // 相对认证根
pub fn target_url(self, url: &str) -> Result<Self, XxxError>     // 完整 URL

// 双地址领域（COPY / MOVE）
pub fn source_path(self, path: &str) -> Result<Self, XxxError>   // 源，相对认证根
pub fn source_url(self, url: &str) -> Result<Self, XxxError>     // 源，完整 URL
pub fn target_path(self, path: &str) -> Result<Self, XxxError>   // 目标，相对认证根
pub fn target_url(self, url: &str) -> Result<Self, XxxError>     // 目标，完整 URL
```

命名理由：

- **不用 `relative_path` / `absolute_path`。** 那两个名字只表达了「相对还是绝对」，没表达「这是哪个地址」。COPY / MOVE 有两个地址，`relative_path` 出现在源上是歧义的，读者必须记住「先设的是源」。
- **不用 `destination_*`。** `destination` 是 HTTP 头的名字，不是调用方脑子里的概念；而且它逼着源也有个对偶名（`source_*`），否则不对称。直接用 `source_*` / `target_*`，两个地址一次说清。
- **单地址领域也用 `target_path`**，叫法收敛为一套，调用方不必区分「这个方法叫 relative_path、那个叫 target_path」。

**不沿用 GET 的 `relative_url(String)`**，因为 GET 那套用 `.expect()` 在非法路径上 panic，且签名形状与 PUT 不一致。新领域统一返回 `Result`，调用方决定怎么处理。

`target_url` / `source_url` 只检查语法、不检查协议：`ftp://` 这类地址能通过 `build()`，但发不出去，`send()` 会以 `Request` 错误报出。理由同 PUT——不和服务端/reqwest 造两套规则。

以 `/` 开头的相对路径会从域名根开始解析，覆盖认证根地址里已有的路径前缀。这是 `Url::join` 的既有语义，与 GET / PUT 一致，本方案不改也不额外拦截。

### 3.4 请求头 API

```rust
pub fn header(self, name: &str, value: &str) -> Result<Self, XxxError>
pub fn headers(self, headers: HeaderMap) -> Self
```

- `header` 返回 `Result`，因为要从 `&str` 现场解析 `HeaderName` / `HeaderValue`
- `headers` 不返回 `Result`，因为 `HeaderMap` 里的名称与取值在构造时已校验过
- 同名请求头是**替换**语义：后设的覆盖先设的，不追加第二个同名头（`HeaderMap::insert` / `extend` 的默认行为）

这套语义与 PUT 完全一致，PUT 领域的既有测试已经把它钉死。

### 3.5 响应处理：一律原样透传

```rust
pub async fn send(self) -> Result<Response, XxxError>
```

- 状态码、响应头、响应体全部原样交给调用方
- 库不把 4xx/5xx 当错误，也不把 2xx 当成功
- 207 Multi-Status 不做特殊处理（`copy` 另提供可选的 `send_and_deserialize`，但不影响 `send` 的行为）
- HEAD 的响应没有响应体，但 `Content-Length` 等响应头仍可正常读取

### 3.6 错误类型

每领域一个 `thiserror` 错误枚举，变体按实际需要裁剪：

```rust
#[derive(Debug, Error)]
pub enum XxxError {
    #[error("URL 格式错误: {0}")]      Url(#[from] url::ParseError),
    #[error("构建 HTTP 请求失败: {0}")] Request(#[from] reqwest::Error),
    #[error("请求头名称格式错误: {0}")] HeaderName(#[from] reqwest::header::InvalidHeaderName),
    #[error("请求头值格式错误: {0}")]   HeaderValue(#[from] reqwest::header::InvalidHeaderValue),
}
```

各领域的增量变体见第四、五章。

### 3.7 名字冲突处理

Builder 转成 `Request` 时用 `client.request(method, url)`，不需要构造 `http::Request`，因此**不存在方法名冲突**。

约定：

- 凡是「要操作的那个地址」（单地址领域），一律叫 `target_path` / `target_url`
- 凡是「源地址」（COPY / MOVE），一律叫 `source_path` / `source_url`
- 凡是「目标地址」（COPY / MOVE），一律叫 `target_path` / `target_url`
- 不使用 `self_` 这类别扭的回避式命名

### 3.8 HTTP 方法构造

非标准方法用 `Method::from_bytes(b"MKCOL")`。reqwest 对自定义方法是透传的，能正常发出。

---

## 四、无请求体组：DELETE / HEAD / MKCOL / OPTIONS

### 4.1 DELETE

```rust
pub enum DeleteDepth { Zero, Infinity }              // 默认 Infinity
impl DeleteDepth { pub fn as_str(self) -> &'static str }

pub struct DeleteBuilder {
    client: Client,
    base_url: Url,
    target_path: Url,
    depth: DeleteDepth,
    headers: HeaderMap,
}

impl DeleteBuilder {
    pub fn new(client: Client, base_url: Url) -> Self
    pub fn target_path(self, path: &str) -> Result<Self, DeleteError>
    pub fn target_url(self, url: &str) -> Result<Self, DeleteError>
    pub fn depth(self, depth: DeleteDepth) -> Self
    pub fn header(self, name: &str, value: &str) -> Result<Self, DeleteError>
    pub fn headers(self, headers: HeaderMap) -> Self
    pub fn build(self) -> Result<Request, DeleteError>
    pub async fn send(self) -> Result<Response, DeleteError>
}
```

- 默认发 `Depth: infinity`：递归删集合是 WebDAV 的实际默认期望
- `build()` 是同步的（无 I/O），与 GET 一致
- 不需要 request body，因此错误枚举里没有 body 相关变体

### 4.2 HEAD

形状与 DELETE 相同，去掉 `depth`：

```rust
pub struct HeadBuilder { client, base_url, target_path, headers }
impl HeadBuilder {
    pub fn new(client: Client, base_url: Url) -> Self
    pub fn target_path(self, path: &str) -> Result<Self, HeadError>
    pub fn target_url(self, url: &str) -> Result<Self, HeadError>
    pub fn header / headers
    pub fn build(self) -> Result<Request, HeadError>          // Method::HEAD
    pub async fn send(self) -> Result<Response, HeadError>
}
```

路径语义与 GET 完全一致，区别只是没有响应体。响应头（`Content-Length`、`Content-Type`、`Last-Modified`、`ETag`）全部保留，调用方能读到。

### 4.3 MKCOL

在 HEAD 的基础上加一个可选请求体：

```rust
pub fn body(self, body: impl Into<String>) -> Self
```

行为规则：

| 情况 | 发出的请求 |
|---|---|
| 不设 body | 零长度请求体，**不发 `Content-Type`** |
| 设了 body | 带 `Content-Type: application/xml; charset=utf-8` |

- 方法名用 `Method::from_bytes(b"MKCOL")`
- 库**不校验** body 是不是合法 XML。理由与 PUT 不拦 `ftp://` 相同：不和服务端造两套规则；RFC 4918 §9.3.1 要求 body 必须是 XML，服务端会自己回 415
- 空字符串等价于不设 body，不单独设错误变体
- 不提供递归建目录能力。413/405/409 等状态码原样透传

### 4.4 OPTIONS

在 HEAD 的基础上加能力解析：

```rust
pub struct OptionsCapabilities {
    pub dav_levels: Vec<String>,        // DAV: "1, 2, 3" → ["1","2","3"]
    pub allowed_methods: Vec<String>,   // Allow: "GET, PUT" → ["GET","PUT"]
}

impl OptionsCapabilities {
    pub fn from_headers(headers: &HeaderMap) -> Self
}

impl OptionsBuilder {
    pub async fn send(self) -> Result<Response, OptionsError>
    pub async fn send_capabilities(self)
        -> Result<(Response, OptionsCapabilities), OptionsError>
}
```

解析规则：

- 逗号分隔，每项 `trim` 后去空项
- 头缺失、头值非 UTF-8、头值为空 → 返回空 `Vec`，**不报错**
- `DAV` 头和 `Allow` 头互相独立，任一缺失不影响另一个

两个入口并存：`send()` 给想看原始响应的调用方，`send_capabilities()` 给想直接用结构化结果的调用方。库只解析、不判定——「支不支持 DAV:3」「能不能 PROPPATCH」由调用方自己读 `dav_levels` / `allowed_methods` 判断。

不设置路径时，请求目标就是认证根地址本身。

---

## 五、带目标地址组：COPY / MOVE

这两个方法多一个「目标资源」，因此比第四章的 4 个领域复杂。

### 5.1 `Overwrite` 协议值类型

由 `copy` 定义：

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum Overwrite {
    #[default]
    True,
    False,
}

impl Overwrite { pub fn as_str(self) -> &'static str }   // "T" / "F"
```

**不使用 `bool`。** 这是 PUT 撤回复盘中第一条教训的直接应用：不要让裸基础类型承载协议值。`bool` 无法表达「这个头根本没发」，也无法在将来扩展第三态。

**`Overwrite::True`（默认）时不发该头。** RFC 4918 §10.6 规定该头的默认值就是 `T`，不发等于告诉服务端「按默认来」，避免发一个服务端本来就能自己推断的头。只有显式设为 `False` 时才写出 `Overwrite: F`。

### 5.2 COPY

```rust
pub enum CopyDepth { Zero, One, Infinity }       // 默认 Infinity

pub struct CopyBuilder {
    client: Client,
    base_url: Url,
    source_path: Url,
    target_path: Option<Url>,
    depth: CopyDepth,
    overwrite: Overwrite,
    headers: HeaderMap,
}

impl CopyBuilder {
    pub fn new(client: Client, base_url: Url) -> Self

    // 源
    pub fn source_path(self, path: &str) -> Result<Self, CopyError>   // 相对认证根
    pub fn source_url(self, url: &str) -> Result<Self, CopyError>     // 完整 URL

    // 目标
    pub fn target_path(self, path: &str) -> Result<Self, CopyError>   // 相对认证根
    pub fn target_url(self, url: &str) -> Result<Self, CopyError>     // 完整 URL

    pub fn depth(self, depth: CopyDepth) -> Self
    pub fn overwrite(self, overwrite: Overwrite) -> Self
    pub fn header / headers

    pub fn build(self) -> Result<Request, CopyError>
    pub async fn send(self) -> Result<Response, CopyError>
    pub async fn send_and_deserialize(self) -> Result<MultiStatus, CopyError>
}
```

#### `Destination` 头的生成规则（本方案唯一的技术难点）

**`Destination` 头的值从已解析的 `Url` 重新序列化，绝不原样回填调用方传入的字符串。**

```text
调用方写：  .target_path("备份/报告 2026.pdf")
库里出去：  Destination: https://dav.example.com/remote.php/dav/files/alice/%E5%A4%87%E4%BB%BD/%E6%8A%A5%E5%91%8A%202026.pdf
```

这样做的两个理由：

1. **自动百分号编码**：中文和空格由 `Url` 负责编码，调用方不需要关心
2. **永远带 scheme + authority**：满足 RFC 4918 §9.9.3 对 Destination 必须是完整 URI 的要求

#### 缺目标时的行为

`build()` 阶段直接返回 `CopyError::MissingTarget`，**不发出一个没有 Destination 头的请求**。漏设目标属于调用方错误，让它尽早暴露。

#### 207 响应

`send_and_deserialize()` 只在 207 时反序列化，非 207 返回 `CopyError::UnexpectedStatus(StatusCode)`——与 `PropFindError::UnexpectedStatus` 同款。

反序列化复用 `lib.rs` 已公开再导出的 `MultiStatus`，**不新建重复模型、不改 `MultiStatus`**。

### 5.3 MOVE

COPY 的同构副本：

```rust
pub enum MoveDepth { Zero, One, Infinity }       // 默认 Infinity

pub struct MoveBuilder { /* 字段与 CopyBuilder 同构 */ }

impl MoveBuilder {
    pub fn new(client: Client, base_url: Url) -> Self
    pub fn source_path / source_url
    pub fn target_path / target_url
    pub fn depth(self, depth: MoveDepth) -> Self
    pub fn overwrite(self, overwrite: Overwrite) -> Self      // 复用 crate::copy::builder::Overwrite
    pub fn header / headers
    pub fn build(self) -> Result<Request, MoveError>          // Method::MOVE
    pub async fn send(self) -> Result<Response, MoveError>
    pub async fn send_and_deserialize(self) -> Result<MultiStatus, MoveError>
}
```

**对外入口名是 `mv()`**，调用方写 `auth.mv()`。模块名 `r#move` 是实现细节（`move` 是 Rust 关键字），不对外暴露：`lib.rs` 里放私有模块 `r#move`，只再导出 `MoveBuilder` / `MoveDepth` / `MoveError`，调用方看不到 `r#`。

命名理由：`mv` 是 `COPY` / `MOVE` 这一组里唯一没有更好选择的缩写（`copy` 就用全名，因为 `cp` 反而更不清晰），且与 Unix `mv` 命令的语义完全一致——移动 + 重命名 + 覆盖，一件事。

MOVE 和 COPY 用**完全相同**的源/目标方法名（`source_path` / `source_url` / `target_path` / `target_url`），调用方在两个动词间切换时不需要重新记名字。

「MOVE 执行完源就不存在」这件事不靠方法名提示，写在文档里：**`source_path` 指定的资源会在请求成功后消失**；要确认结果就用 `head()` 或 `propfind()` 回查，不要假设。

### 5.4 增量错误变体

```rust
// CopyError / MoveError 在通用四变体之外增加：
#[error("未设置目标地址")]
MissingTarget,

#[error("期望 207 Multi-Status，实际收到 {0}")]
UnexpectedStatus(StatusCode),

#[error("反序列化失败: {0}")]
De(#[from] quick_xml::DeError),
```

---

## 六、对外导出

### 6.1 `src/lib.rs` 的改法

只改 6 个未实现模块的可见性，追加 6 行再导出：

```rust
// ── 已实现领域：模块私有，只经下方再导出对外 ──
mod auth;
mod get;
mod propfind;
mod put;

// ── 本次实现的领域：同样收口为私有 ──
mod copy;
mod delete;
mod head;
mod mkcol;
mod options;

/// `move` 是 Rust 关键字，使用原始标识符导出 WebDAV MOVE 模块。
mod r#move;

// ── PROPPATCH 由项目所有者实现，保持 pub mod 不变 ──
pub mod proppatch;

// ── 本次新增的再导出 ──
pub use copy::builder::{CopyBuilder, CopyDepth, CopyError, Overwrite};
pub use delete::builder::{DeleteBuilder, DeleteDepth, DeleteError};
pub use head::builder::{HeadBuilder, HeadError};
pub use mkcol::builder::{MkcolBuilder, MkcolError};
pub use move::builder::{MoveBuilder, MoveDepth, MoveError};
pub use options::builder::{OptionsBuilder, OptionsCapabilities, OptionsError};
```

`r#move` 的再导出路径按 Rust 模块系统的实际要求写（`move::builder::…`），编译验证后再定稿。

**已实现四个领域的 `mod` 声明与全部既有再导出，一行不动。** `pub mod proppatch;` 保持原样。

### 6.2 `src/auth.rs` 的改法

在文件末尾的 `impl WebdavAuth` 块里**只追加** 6 个工厂方法，既有 3 个工厂方法与其它内容一行不改：

```rust
impl WebdavAuth {
    pub fn copy(&self) -> CopyBuilder {
        CopyBuilder::new(self.client.clone(), self.base_url.clone())
    }

    pub fn delete(&self) -> DeleteBuilder {
        DeleteBuilder::new(self.client.clone(), self.base_url.clone())
    }

    pub fn head(&self) -> HeadBuilder {
        HeadBuilder::new(self.client.clone(), self.base_url.clone())
    }

    pub fn mkcol(&self) -> MkcolBuilder {
        MkcolBuilder::new(self.client.clone(), self.base_url.clone())
    }

    pub fn options(&self) -> OptionsBuilder {
        OptionsBuilder::new(self.client.clone(), self.base_url.clone())
    }

    pub fn mv(&self) -> MoveBuilder {
        MoveBuilder::new(self.client.clone(), self.base_url.clone())
    }
}
```

全部走 `self.client.clone()` + `self.base_url.clone()`，与既有三个工厂同款，Authorization 自动复用。

**验收方法**：`git diff src/auth.rs` 输出应当只有 `+` 号行，没有任何 `-` 号行。

---

## 七、文件清单

### 7.1 新增源码（6 个）

```text
src/copy/builder.rs
src/delete/builder.rs
src/head/builder.rs
src/mkcol/builder.rs
src/options/builder.rs
src/move/builder.rs
```

### 7.2 新增测试（6 套目录）

```text
tests/{copy,delete,head,mkcol,options,move}/
```

### 7.3 新增示例

```text
examples/local_workflow.rs       完整本地闭环（6 个领域串一遍）
```

### 7.4 修改（只有 3 个文件）

| 文件 | 改动 |
|---|---|
| `Cargo.toml` | 追加 6 个 `[[test]]` 条目，既有 4 条原样保留 |
| `src/lib.rs` | 6 个未实现模块 `pub mod` → 私有 `mod`，追加 6 行 `pub use` |
| `src/auth.rs` | 末尾追加 6 个工厂 fn，只加不减 |

---

## 八、测试方案

### 8.1 目录结构

严格照 `docs/测试/测试规范.md`，每个领域一个独立测试目标，与 `tests/get/` 完全同构：

```text
tests/<domain>/
  mod.rs                      # mod common; mod local; mod support;
                              # #[cfg(feature = "network-test")] mod network;
  common.rs
  common/local_http.rs        # MockServer 启动 + base_url 生成
  common/network_config.rs    # WEBDAV_URL / WEBDAV_ACCOUNT / WEBDAV_PASSWORD
  support.rs
  support/fixtures.rs         # 本领域专用夹具
  local.rs
  local/unit.rs
  local/unit/boundary.rs      + boundary/<topic>.rs
  local/unit/serialization.rs + serialization/<topic>.rs
  local/unit/whitebox.rs      + whitebox/<topic>.rs
  local/integration.rs
  local/integration/request_contract.rs
  local/integration/response_status.rs
  network.rs
  network/<name>.rs
```

`common/` 与 `support/` **每个领域各留一份，不跨领域共用**——这是测试规范明确要求的（跨目录共用只能靠 `#[path]`，而项目禁止 `#[path]`）。`common/` 只放通用 HTTP Mock、环境变量读取和通用断言，不承载领域规则。

测试名采用 `场景_预期结果` 形式，例如 `missing_target_returns_error`。测试之间不依赖执行顺序、不依赖其他测试创建的数据。

### 8.2 各领域覆盖要点

**DELETE**

- `DeleteDepth::Zero` 与 `Infinity` 的 `Depth` 头取值
- 默认发 `Depth: infinity`
- 路径编码（中文/空格）、`target_path` 与 `target_url` 两种设置方式、`/` 开头路径覆盖根前缀
- 状态码 204 / 404 / 423 / 207 逐码透传
- 自定义请求头落到服务端
- 删除后再 PROPFIND 应查不到（集成）

**HEAD**

- 方法为 HEAD、路径语义与 GET 一致
- 响应体为空但 `Content-Length` / `Content-Type` / `ETag` 可读
- 404 原样透传
- 与 GET 对同一路径的响应头一致性

**MKCOL**

- 不设 body → 零长度请求体且**无 `Content-Type`**
- 设 body → `Content-Type: application/xml; charset=utf-8` 且 body 字节一致
- 空字符串等价于不设 body
- 201 / 405 / 409 / 415 逐码透传
- 路径编码

**OPTIONS**

- `DAV` 头解析：`"1, 2, 3"` 带空格、`"1,2"` 无空格、`"1"` 单值、空值、头缺失
- `Allow` 头同上
- 两个头互相独立（只有一个存在时另一个为空 `Vec`）
- 头值非 UTF-8 的边界
- `send()` 与 `send_capabilities()` 两个入口结果一致
- 不设路径时请求目标为认证根

**COPY**

- Destination 头从 URL 重新生成：中文/空格百分号编码、带 scheme + authority
- `Overwrite::False` 才写出 `Overwrite: F`；`Overwrite::True` 时该头**不出现**
- `CopyDepth` 三态的 `Depth` 头取值
- 缺 target → `MissingTarget` 错误，且不发出请求
- `source_path` / `source_url` / `target_path` / `target_url` 各自的路径语义与编码
- `source_*` 与 `target_*` 互不干扰（设了源不影响目标，反之亦然）
- 201 / 204 / 207 / 412 / 409 逐码透传
- 207 经 `send_and_deserialize()` 解析，非 207 报 `UnexpectedStatus`

**MOVE**

- 与 COPY 同构的上述全部要点
- `Method::MOVE`
- 与 COPY 用完全相同的源/目标方法名
- 复用 `Overwrite` 类型
- 移动后源消失、目标出现（集成）

**whitebox 通用**：每个 Builder 的默认值、路径覆盖、头替换语义、非法头名/头值错误分支、不可达服务器时的 `send()` 错误分支。

### 8.3 验收门槛

沿用测试规范的既有门槛：

- 行覆盖率 ≥ 95%
- 函数覆盖率 ≥ 95%
- 区域覆盖率 ≥ 95%
- 6 个领域的关键成功分支与错误分支全部命中
- 文档示例全部通过编译

---

## 九、真实服务器验收

真实服务器由项目所有者提供，专用于测试。**授权范围与红线如下。**

### 9.1 红线：服务端既有内容一律不可改动

- **不删除任何既有资源**
- **不覆盖任何既有文件名**（COPY 到已存在路径、PUT 覆盖既有文件，都禁止）
- **不移动任何既有资源**
- **不改任何既有资源的属性**
- 不批量遍历、不试探性删除

### 9.2 自建命名空间

所有写操作只在自己创建的命名空间内发生：

```text
__webdav_core_selftest_<时间戳>_<进程号>/
```

- 名字唯一，不会与既有内容冲突，也不会与上一次运行的残留冲突
- 目录、子目录、文件全部由用例自己创建
- 用例结束时自行清理整个命名空间

### 9.3 验证方式：用 PROPFIND 核对服务端真实状态

不只断言状态码，而是读回服务端自己的记录来确认操作真的生效：

| 操作 | 核对方式 |
|---|---|
| MKCOL | PROPFIND 该路径，`<resourcetype><collection/>` 出现 |
| COPY | PROPFIND 目标路径，长度/ETag 与源一致；源仍在 |
| MOVE | PROPFIND 源路径查不到，目标路径查得到 |
| DELETE | PROPFIND 该路径查不到（404 或不在列表中） |
| HEAD | 与 PROPFIND 得到的 `getcontentlength` 一致 |
| OPTIONS | 返回的 `DAV` 头能解析出非空 `dav_levels` |

### 9.4 执行方式

真实用例同时使用 `network-test` 功能开关与 `#[ignore]`：

```text
cargo test --features network-test --test <domain> -- --ignored --nocapture
```

- 需要 `WEBDAV_URL`、`WEBDAV_ACCOUNT`、`WEBDAV_PASSWORD`
- 输出中不出现账号、密码、Authorization
- 清理失败只打印残留名字，不对残留做断言，也不掩盖失败
- 本地测试全绿之后才执行

---

## 十、目标效果：完整用法

以下是目标 API 的使用效果。**这是设计目标，不是已实现代码。**

### 10.1 HEAD：只问元数据，不下载内容

```rust
let response = auth
    .head()
    .target_path("Documents/report.pdf")?
    .send()
    .await?;

if response.status() == StatusCode::NOT_FOUND {
    println!("文件不存在");
} else {
    let length = response
        .headers()
        .get("content-length")
        .and_then(|v| v.to_str().ok())
        .and_then(|v| v.parse::<u64>().ok());

    println!(
        "状态={} 大小={:?} 类型={:?} 修改时间={:?}",
        response.status(),
        length,
        response.headers().get("content-type"),
        response.headers().get("last-modified"),
    );
}
```

### 10.2 OPTIONS：探测服务端能力

```rust
let (response, caps) = auth.options().send_capabilities().await?;

println!("HTTP {}", response.status().as_u16());
println!("DAV 级别: {:?}", caps.dav_levels);        // ["1", "2", "3"]
println!("允许方法: {:?}", caps.allowed_methods);    // ["OPTIONS", "GET", "PUT", "MOVE", ...]

if !caps.allowed_methods.iter().any(|m| m == "MOVE") {
    println!("服务端不支持 MOVE，跳过移动步骤");
}

// 只看原始响应：
let raw = auth.options().send().await?;
println!("原始 DAV 头: {:?}", raw.headers().get("dav"));
```

### 10.3 MKCOL：建目录

```rust
let response = auth
    .mkcol()
    .target_path("release/2026-Q1/")?
    .send()
    .await?;

match response.status().as_u16() {
    201 => println!("创建成功"),
    405 => println!("已存在"),               // 状态码原样给你，库不判定
    _ => println!("创建失败: {}", response.status()),
}
```

带扩展 XML body（库不校验 XML，只负责按 XML 发）：

```rust
let response = auth
    .mkcol()
    .target_path("extended/")?
    .body(
        r#"<D:mkcol xmlns:D="DAV:"><D:set><D:prop><D:resourcetype><D:collection/></D:resourcetype></D:prop></D:set></D:mkcol>"#,
    )
    .send()
    .await?;
```

### 10.4 COPY：复制，目标另起名字

```rust
let response = auth
    .copy()
    .source_path("staging/report.pdf")?                 // 源
    .target_path("release/2026-Q1/report.pdf")?        // 目标，相对认证根
    .overwrite(Overwrite::False)                        // 明确禁止覆盖已有正式版
    .send()
    .await?;

match response.status().as_u16() {
    201 => println!("已复制"),
    204 => println!("目标已存在并被覆盖"),
    207 => println!("部分成功，用 send_and_deserialize() 看细节"),
    412 => println!("目标已存在，且你设了不允许覆盖"),
    _ => println!("复制失败: {}", response.status()),
}
```

要读 207 细节：

```rust
let multistatus = auth
    .copy()
    .source_path("a/")?
    .target_path("b/")?
    .send_and_deserialize()
    .await?;

for item in &multistatus.response {
    println!(
        "{}: {:?}",
        item.href,
        item.propstat.first().map(|p| &p.status)
    );
}
```

### 10.5 MOVE：重命名 / 移动

```rust
let response = auth
    .mv()
    .source_path("staging/report.pdf")?
    .target_path("archive/2026-Q1/report.pdf")?
    .overwrite(Overwrite::False)
    .send()
    .await?;

println!("移动结果: {}", response.status());

// 用 HEAD 自己核对，而不是相信状态码
let old_path = auth.head().target_path("staging/report.pdf")?.send().await?;
let new_path = auth.head().target_path("archive/2026-Q1/report.pdf")?.send().await?;
println!(
    "源还在吗={} 目标在吗={}",
    old_path.status().is_success(),
    new_path.status().is_success()
);
```

### 10.6 DELETE：删除

```rust
// 删之前先看里面有什么
let listing = auth
    .propfind()
    .path("staging/")?
    .depth(Depth::One)
    .props([FindProp::Resourcetype, FindProp::Getcontentlength])
    .send_and_deserialize()
    .await?;

for item in &listing.response {
    println!("将删除: {}", item.href);
}

let response = auth
    .delete()
    .target_path("staging/")?
    .depth(DeleteDepth::Infinity)          // 只删当前层用 DeleteDepth::Zero
    .send()
    .await?;

match response.status().as_u16() {
    204 => println!("删除完成"),
    207 => println!("部分失败"),
    423 => println!("资源被锁住，删不掉"),
    _ => println!("删除失败: {}", response.status()),
}
```

### 10.7 六个领域串成完整闭环

```rust
/// 典型场景：把 staging/ 里的东西转成正式版。
/// 每一步都用真实状态核对，不靠状态码猜。
async fn promote(auth: &WebdavAuth) -> Result<(), Box<dyn std::error::Error>> {
    // 1. OPTIONS：先问服务端支不支持我们要用的方法
    let (_, caps) = auth.options().send_capabilities().await?;
    if !caps.allowed_methods.iter().any(|m| m == "MOVE") {
        return Err("服务端不支持 MOVE".into());
    }

    // 2. MKCOL：目标目录可能还不存在（405 表示已存在）
    let created = auth.mkcol().target_path("release/2026-Q1/")?.send().await?;
    println!("MKCOL -> {}", created.status());

    // 3. HEAD：先确认源文件在、有多大
    let probe = auth.head().target_path("staging/report.pdf")?.send().await?;
    if !probe.status().is_success() {
        return Err(format!("源文件不可读: {}", probe.status()).into());
    }
    println!("源大小 = {:?}", probe.headers().get("content-length"));

    // 4. COPY：先在 release 留一份，禁止覆盖已有的正式版
    let copied = auth
        .copy()
        .source_path("staging/report.pdf")?
        .target_path("release/2026-Q1/report.pdf")?
        .overwrite(Overwrite::False)
        .send()
        .await?;
    println!("COPY -> {}", copied.status());

    // 5. MOVE：把 staging 那份归档，源随之消失
    let moved = auth
        .mv()
        .source_path("staging/report.pdf")?
        .target_path("archive/2026-Q1/report.pdf")?
        .send()
        .await?;
    println!("MOVE -> {}", moved.status());

    // 6. PROPFIND：用服务端自己的记录核对最终状态
    let tree = auth
        .propfind()
        .path("release/2026-Q1/")?
        .depth(Depth::One)
        .props([FindProp::Getcontentlength, FindProp::Resourcetype])
        .send_and_deserialize()
        .await?;
    for item in &tree.response {
        let prop = item.propstat.first().map(|p| &p.prop);
        println!(
            "{} 大小={:?}",
            item.href,
            prop.and_then(|p| p.content_length)
        );
    }

    // 7. DELETE：清理 release 里的临时副本（只删自己建的）
    let removed = auth
        .delete()
        .target_path("release/2026-Q1/report.pdf")?
        .depth(DeleteDepth::Zero)
        .send()
        .await?;
    println!("DELETE -> {}", removed.status());

    Ok(())
}
```

### 10.8 六个领域共用的手感

| 维度 | 统一规则 |
|---|---|
| 路径 | 单地址领域有 `target_path` / `target_url`；COPY / MOVE 另有 `source_path` / `source_url`。全部返回 `Result`，都不 panic |
| 地址命名 | 源一律 `source_*`，目标一律 `target_*`，6 个领域一套名字 |
| 请求头 | 全部有 `header(&str, &str) -> Result` 与 `headers(HeaderMap)`，替换语义 |
| 发送 | 全部是 `async fn send(self) -> Result<Response, XxxError>` |
| 状态码 | 一律原样透传，库不判定成功失败 |
| 错误 | 每领域一个 `thiserror` 枚举，构造期错误与发送期错误可区分 |
| 入口 | 全部可从 `WebdavAuth` 直接派生，Authorization 自动复用 |

调用方不需要为不同方法记不同规则。

---

## 十一、实施顺序

每个阶段完成即验证，不跨阶段重构。

| 阶段 | 内容 | 验证闸门 |
|---|---|---|
| 1 | `lib.rs` 收口准备：确认 6 个模块的私有化不影响既有编译 | 既有 160 个测试零回归 |
| 2 | DELETE：`builder.rs` + 测试 + `lib.rs` 再导出 + `auth.rs` 工厂 | `cargo test --test delete` 全绿 |
| 3 | HEAD | `cargo test --test head` 全绿 |
| 4 | MKCOL | `cargo test --test mkcol` 全绿 |
| 5 | OPTIONS | `cargo test --test options` 全绿 |
| 6 | COPY（含 `Overwrite` / `CopyDepth`） | `cargo test --test copy` 全绿 |
| 7 | MOVE | `cargo test --test move` 全绿 |
| 8 | 真实网络验收（6 个领域） | 服务器既有内容零改动；PROPFIND 核对通过 |
| 9 | `examples/local_workflow.rs` + `cargo test --doc` | 编译通过、文档示例通过 |
| 10 | `cargo-llvm-cov` 复查 | 行/函数/区域覆盖率均 ≥ 95% |
| 11 | 复查 `git status` / `git diff` | 只碰文件清单内的文件；`git diff src/auth.rs` 只有 `+` 号 |

**关于阶段 1 的一个说明**：Builder 类型与工厂方法是同一编译单元，不能先加工厂再加类型。因此实际操作是**每个阶段实现该领域时同步追加它对应的那一行工厂与再导出**，而不是第 1 阶段一次性加 6 个。

**提交纪律**：不做 `git add`，不做 `git commit`，不做 `git push`，除非获得单独明确授权。

---

## 十二、最终验收标准

1. 既有 160 个本地测试零回归
2. 6 个领域的本地测试全部通过，且可单跑（`cargo test --test <domain>`）
3. 每个公开函数都有对应的成功用例与边界/错误用例
4. `cargo test --doc` 通过，`examples/local_workflow.rs` 编译通过
5. 行覆盖率、函数覆盖率、区域覆盖率均不低于 95%
6. 真实网络验收通过，且服务器既有内容零改动
7. 已实现四个领域（auth / get / propfind / put）的源码与测试零改动
8. PROPPATCH 相关文件零改动
9. `git diff src/auth.rs` 输出只有新增行

---

## 十三、已定事项与实施记录

### 13.1 已由项目所有者拍板的两条命名

1. **MOVE 的入口名是 `mv()`**：调用方写 `auth.mv()`；模块名 `r#move` 是实现细节，不对外暴露。
2. **源/目标地址命名**：源用 `source_path` / `source_url`，目标用 `target_path` / `target_url`；单地址领域（DELETE / HEAD / MKCOL / OPTIONS）统一用 `target_path` / `target_url`。不引入 `relative_path` / `absolute_path` / `destination_*` / `from_path` 等别名。

### 13.2 实施结果

6 个领域全部实现，本地测试 **453 个全绿**（既有 160 个零回归），覆盖率：

| 指标 | 实测 | 门槛 |
|---|---|---|
| 区域覆盖率 | 95.71% | ≥95% ✅ |
| 函数覆盖率 | 98.79% | ≥95% ✅ |
| 行覆盖率 | 98.61% | ≥95% ✅ |

新增 6 个领域的源码行覆盖率均为 100%。

### 13.3 实施中发现的既有领域约束（**未修改，仅记录**）

实施 COPY / MOVE 的 207 多状态响应解析时发现：`propfind` 领域的
`PropStat.prop` 是**必填**字段（无 `#[serde(default)]`）。因此若服务端返回的某个
`<propstat>` 省略了 `<prop>` 子元素，整个响应会解析失败。

本方案的处理：**不改 `propfind`**（它是已实现领域），而是在 `copy` 与 `move` 的
`send_and_deserialize` 文档里写明这条解析前提，并用测试把真实行为固定住
（`multi_status_without_prop_element_is_reported_as_deserialization_error`）。
需要在这种响应上取 `href` 与状态码时，用 `send()` 拿原始响应自行处理。

**待项目所有者决定**：是否给 `PropStat.prop` 加 `#[serde(default)]`。这属于对已实现
领域的改动，本方案未做。

同类记录：`FindProp` 枚举只提供 5 个可选属性（`Resourcetype`、`Creationdate`、
`Getetag`、`Getlastmodified`、`Getcontenttype`），**不含** `getcontentlength`；但
`Prop` 结构体里能反序列化出 `content_length`。因此「按长度筛选属性」在
PROPFIND 请求侧目前做不到，只能在响应解析后读 `prop.content_length`。
`examples/local_workflow.rs` 因此没有演示按长度选择属性。

### 13.4 真实网络验收：未执行，原因是客观阻塞

按第九章节的规则本应执行真实服务验收，但当前环境存在两项硬阻塞：

1. **`WEBDAV_PASSWORD` 环境变量不存在**。环境中只有 `WEBDAV_URL`
   （`https://aki.teracloud.jp/dav/`）与 `WEBDAV_ACCOUNT`；项目内也没有任何凭据文件。
2. **出网被环境阻断**。对该地址发起请求时，目标解析为 `198.18.0.64`，
   连接报 `Os { code: 10013, kind: PermissionDenied }`；直接用 PowerShell 请求
   同一地址则以超时告终。本会话审批已禁用，无法提权放开网络。

因此 `tests/{delete,head,mkcol,options,copy,move}/network/` 下的验收用例**已写好但未运行**。
它们全部带 `network-test` 特性开关与 `#[ignore]`，并在本机 MockServer 上验证过的同一条
代码路径上运行，随时可在具备凭据与出网的环境中执行：

```text
cargo test --features network-test --test <domain> -- --ignored --nocapture
```

这些用例严格遵守第九章节的红线：只在自建命名空间
`__webdav_core_selftest_<领域>_<时间戳>_<进程号>` 内创建与删除，服务端既有内容
一律不碰，且用 GET / HEAD / PROPFIND 回查服务端真实状态来判定结果。
