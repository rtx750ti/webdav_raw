# WebDAV Core 测试详细计划

## 当前基线

当前默认测试集执行 48 个测试并通过。文档示例测试有两个编译失败：`PropFindBuilder::path` 示例缺少完整调用上下文，`PropFindBuilder::props` 示例缺少 builder 和 `FindProp` 定义。

现有测试主要集中在源码文件内，认证、GET 和 PROPFIND 的领域边界尚未在 `tests` 目录中形成独立目标。GET 有本地 MockServer 用例，PROPFIND 有本地 MockServer 用例和两份真实 XML 固件；真实网络测试通过 `network-test` 功能开关读取环境变量。

当前工作区有 `auth.rs` 暂存改动。本计划实施时保持该改动和暂存区状态，不混入测试工作。

## 落地目标

1. 建立认证、GET、PROPFIND 三个独立测试目标。
2. 每个领域按 `local/integration`、`local/boundary`、`local/coverage`、`network` 组织。
3. 单元测试优先覆盖公开接口的边界、序列化、反序列化和白盒路径；集成测试覆盖完整请求流程。
4. 使用覆盖率报告补齐遗漏分支，使业务源码行、函数、区域覆盖率均达到 95%。
5. 真实网络测试只验证配置的 WebDAV 服务兼容性，不影响本地覆盖率结论。

## 领域数据流和测试范围

### 认证领域

```text
用户名、密码、根地址
  → WebdavAuth::new
  → 根地址规范化、Basic Authorization、Client
  → get() 或 propfind()
```

| 层级 | 文件主题 | 覆盖线路 |
|---|---|---|
| 单元边界 | `base_url.rs` | 空地址、格式错误、非 HTTP 协议、无主机、Query、Fragment、根路径、补尾部 `/`、已有尾部 `/` |
| 单元白盒 | `identity.rs` | 相同凭据、不同凭据、不同根地址、Debug 脱敏 |
| 单元白盒 | `auth_branches.rs` | 认证构造链的全部错误返回和成功路径 |
| 本地集成 | `authorization_contract.rs` | 合法认证对象实际发出的 Authorization 请求头 |
| 本地集成 | `factory_contract.rs` | `get()` 与 `propfind()` 使用认证对象的 Client 和根地址 |
| 覆盖补点 | `coverage_gaps.rs` | 覆盖率报告中认证领域仍遗漏的线路 |

认证领域以本地 HTTP 请求验证认证头，因此当前没有必须连接真实 WebDAV 的网络测试。

### GET 领域

```text
WebdavAuth::get 或 GetBuilder::new
  → 默认目标或 relative_url/absolute_url
  → build
  → send
  → 原始 Response
```

| 层级 | 文件主题 | 覆盖线路 |
|---|---|---|
| 单元边界 | `relative_url.rs` | 空路径、中文路径、空格、前导 `/`、`.`、`..`、连续 `/` |
| 单元边界 | `absolute_url.rs` | 合法绝对地址、带端口地址、非法绝对地址 |
| 单元白盒 | `builder_paths.rs` | 默认目标、后续设置覆盖前次设置、请求构建和错误返回路径 |
| 单元白盒 | `transport_error.rs` | 无可用服务时的网络错误映射 |
| 本地集成 | `request_contract.rs` | GET 方法、认证根地址、默认目标、相对地址构建 |
| 本地集成 | `response_success.rs` | 200 响应、响应体、响应头原样返回 |
| 本地集成 | `response_status.rs` | 401、404、500 作为原始响应返回给调用方 |
| 覆盖补点 | `coverage_gaps.rs` | 覆盖率报告中 GET 领域仍遗漏的线路 |
| 真实网络 | `download_existing.rs` | 已存在文件的状态码、Content-Length、流式读取 |
| 真实网络 | `download_missing.rs` | 不存在路径的响应 |
| 真实网络 | `download_wrong_auth.rs` | 错误认证的响应 |

当前 `GetBuilder::new` 使用固定 example.com 初始地址，计划中的 `request_contract.rs` 先锁定默认目标应使用认证根地址的接口约定。若该测试报红，再单独修复 GET 代码。

### PROPFIND 领域

```text
WebdavAuth::propfind 或 PropFindBuilder::new
  → path、Depth、selector
  → PROPFIND XML 请求
  → send 或 send_and_deserialize
  → 207 Multi-Status
  → MultiStatus、Response、PropStat、Prop
```

| 层级 | 文件主题 | 覆盖线路 |
|---|---|---|
| 单元边界 | `depth.rs` | 0、1、infinity 三种值 |
| 单元边界 | `request_path.rs` | 空路径、中文、前导 `/`、点路径、连续 `/` |
| 单元边界 | `selector.rs` | allprop、propname、空属性集、五种属性、重复属性 |
| 单元边界 | `href.rs` | 普通路径、中文百分号路径、非法 UTF-8 编码 |
| 单元边界 | `date.rs` | RFC2822 时间、缺失时间、非法时间 |
| 单元边界 | `mime.rs` | 合法 MIME、缺失 MIME、非法 MIME |
| 单元边界 | `content_length.rs` | 数字、0、空白、缺失、非法数字 |
| 单元边界 | `response_list.rs` | 空列表、仅根节点、根节点加资源、多资源、目录和文件 |
| 单元序列化 | `request_xml.rs` | allprop、propname、指定属性序列化为 XML |
| 单元序列化 | `response_xml_roundtrip.rs` | XML 反序列化、模型序列化、再次反序列化后的语义一致性 |
| 单元白盒 | `builder_paths.rs` | URL 拼接、请求构建、207 判断的独立路径 |
| 单元白盒 | `parser_paths.rs` | XML 反序列化、字段转换、错误返回的独立路径 |
| 本地集成 | `allprop_contract.rs` | allprop XML、Depth、请求头、207 正常响应 |
| 本地集成 | `propname_contract.rs` | propname XML、请求头、207 正常响应 |
| 本地集成 | `properties_contract.rs` | 指定属性、去重、稳定顺序、请求 XML |
| 本地集成 | `response_207.rs` | 207 XML 到 MultiStatus 的完整结果 |
| 本地集成 | `response_status.rs` | 200、401、404、500 映射为 UnexpectedStatus |
| 覆盖补点 | `coverage_gaps.rs` | 覆盖率报告中 PROPFIND 领域仍遗漏的线路 |
| 真实网络 | `list_directory.rs` | 目录 PROPFIND 与响应结构 |
| 真实网络 | `list_depth_zero.rs` | Depth=0 行为 |
| 真实网络 | `list_depth_one.rs` | Depth=1 行为 |
| 真实网络 | `list_missing_path.rs` | 不存在路径错误响应 |
| 真实网络 | `list_wrong_auth.rs` | 错误认证响应 |

两份 XML 固件都进入 PROPFIND 本地测试。固件测试至少断言 href 解码、目录或文件、内容长度、最后修改时间、MIME、ETag 和响应数量。

`MultiStatus::from_str` 已保留服务端返回的完整响应。`response_xml_roundtrip.rs` 以完整响应验证双向转换；`response_list.rs` 验证空列表、仅根节点和多资源列表的原始解析结果。后续如需筛除当前目录，单独提供列表处理能力并为它编写测试。

## 文档和示例

| 文件主题 | 验收内容 |
|---|---|
| `PropFindBuilder::path` 文档示例 | 完整创建 builder 后调用 `path`，可以独立编译 |
| `PropFindBuilder::props` 文档示例 | 包含 builder 与 `FindProp` 的完整定义或导入，可以独立编译 |
| `fetch_get` example | 编译通过，不在默认测试中连接真实服务 |
| `fetch_propfind` example | 编译通过，不在默认测试中连接真实服务 |

## 实施顺序

1. 在 Cargo 中注册 `auth`、`get`、`propfind` 独立测试目标，建立目录和空模块入口。
2. 建立 `tests/common` 的本地 HTTP 工具和真实网络配置读取；建立 GET、PROPFIND 各自的领域辅助代码。
3. 迁移认证的现有测试，优先完成认证单元边界和白盒路径。
4. 迁移 GET 的现有测试，优先完成地址边界和 Builder 白盒路径，再运行默认目标合同测试。
5. 迁移 PROPFIND 请求构建测试，优先完成请求 XML 序列化、响应 XML 反序列化和双向回环。
6. 用两份 XML 固件补全 PROPFIND 字段边界、白盒路径和语义回环测试。
7. 修复并验证两个文档示例，补 example 编译检查。
8. 接入本地覆盖率报告；每次报告只增加对应领域的 `coverage` 用例，直至达到门槛。
9. 整理真实网络测试为 `network-test` 加 `#[ignore]`，在受控环境进行只读验收。

## 验收清单

- `auth`、`get`、`propfind` 可以分别执行本地测试。
- 断开网络和清除 WebDAV 环境变量后，本地测试仍通过。
- 真实网络测试未启用时不会读取环境变量或建立外部连接。
- 两份 PROPFIND XML 固件都有字段断言。
- PROPFIND 模型经过序列化和再次反序列化后，资源数量、顺序和已支持字段保持一致。
- 所有文档示例和 examples 编译通过。
- 本地覆盖率的行、函数、区域均达到 95%。
- 覆盖补点测试均能对应覆盖率报告中的具体遗漏。
- 测试不修改远端资源，不输出凭据，不改变当前暂存区，不执行 `git push`。
