# 全覆盖工作流集成测试方案

本文档定义一套**打在真实 WebDAV 服务器上**的集成测试。它按一条完整的业务链路依次使用
本库的全部能力，并在每一步用服务端自己的记录核对结果，用来回答一个具体问题：

> 这些功能在真实服务器上到底管不管用？

**本文档只定义测试方案。** 文档先行的原因是：这套测试会对真实服务器做写操作，步骤、
断言、清理责任必须先固定下来再实施。

---

## 一、定位：为什么再写一套

已有测试已经覆盖了各领域的**局部契约**：

| 已有测试 | 覆盖范围 |
|---|---|
| `tests/<领域>/local/unit` | 边界、序列化、白盒分支 |
| `tests/<领域>/local/integration` | 单领域的请求契约与状态码透传 |
| `tests/<领域>/network` | 单领域在真实服务器上的最小验收 |

它们各自的领域内是够的，但**没有任何一套把领域串起来**。串起来才会暴露单领域测试
天然看不到的问题：前一个动作产生的状态是否被后一个动作正确识别、集合的递归语义在多层
目录下是否自洽、复制与移动对「源」和「目标」的处理是否真的符合直觉。

因此这套测试的定位是**端到端的行为验证**，而不是替代或扩充单元测试。

---

## 二、范围与硬约束

### 2.1 只用真实服务器

**不使用任何 mock / fake / wiremock 服务器。** 这套测试的全部断言都必须建立在真实服务端
返回的内容上。用假服务器验证「功能是否有效」是自欺——它只能验证假服务器实现得对不对。

### 2.2 红线：服务端既有内容一律不动

| 允许 | 禁止 |
|---|---|
| 在自己创建的命名空间内建目录、传文件、复制、移动、删除 | 删除、覆盖、移动、改属性任何既有资源 |
| 用 PROPFIND/HEAD/GET/OPTIONS 读取任意路径（只读） | 对既有资源发任何写方法 |
| 删除自己命名空间内的资源 | 批量遍历后批量删除（只删自己前缀的顶层项） |

### 2.3 命名空间

所有写操作限制在一个自建命名空间内：

```text
__webdav_core_workflow_<时间戳>_<进程号>/
```

- 名字唯一，不会撞到既有内容，也不会撞到上一次运行的残留。
- 内部目录仍按方案要求命名：`测试目录01`、`测试目录02`、`测试目录03`、`测试目录04`。
- 因为目录名含中文，**每个测试函数用独立命名空间**，避免并发运行时互相干扰。

### 2.4 执行纪律

- 测试全部带 `network-test` 特性开关与 `#[ignore]`。
- **人工执行，且一轮只跑一次**；不重复批量重跑（既为服务器负载，也为账号安全）。
- 输出不打印账号、密码或 `Authorization`。

---

## 三、目录与目标

```text
tests/workflow/
  mod.rs                          # 只登记子模块，不放用例
  common.rs
  common/network_config.rs        # 读取 WEBDAV_URL / ACCOUNT / PASSWORD
  support.rs
  support/context.rs              # Ctx：Auth、命名空间、基线快照、步骤打印
  support/dav_tree.rs             # 真实服务器操作助手与断言助手
  network.rs
  network/temp_tree.rs            # 工作流编排（只调用步骤函数）
  network/temp_tree/steps.rs      # 步骤 1–49，每步一个 async fn
  network/temp_tree/negative.rs   # 负向用例 N1–N10，每个一个 async fn
```

`network/temp_tree.rs` 与 `network/temp_tree/steps.rs` 是同一个模块族（Rust 2018+ 的文件
模块布局），`temp_tree.rs` 内用 `mod steps;` 登记，不需要 `#[path]`。

`Cargo.toml` 追加：

```toml
[[test]]
name = "workflow"
path = "tests/workflow/mod.rs"
```

顶层目录名用 `workflow` 而不是 `e2e`：测试规范把 `local/` 定义为离线测试、`network/`
定义为真实验收，这套测试全部属于 `network/`，用 `workflow` 表达「跨领域的工作流」，
不与规范的层级语义冲突。

---

## 四、函数划分：每一步一个函数

### 4.1 原则

**宁可函数多，也不要一个函数做多件事。** 这套测试有 49 个正向步骤与 10 个负向用例，
每一个都是独立函数，函数名直接说明它验证什么。

禁止的写法：

```rust
// 反例：一个函数里塞了建目录、传文件、复制、删除十几件事。
// 失败时只知道"整体完了"，不知道错在哪一步；也没法单独看某一步在干什么。
#[tokio::test]
async fn whole_workflow() {
    // ... 300 行，中间全是动作与断言 ...
}
```

要求的写法：

```rust
// 每个步骤一个函数，名字即文档。
async fn step_07_mkcol_测试目录01(ctx: &Ctx) { /* 只做这一件事 */ }
async fn step_08_verify_测试目录01_is_collection(ctx: &Ctx) { /* 只核对这一件事 */ }

// 编排函数只负责按顺序调用，本身不含业务逻辑。
#[tokio::test]
#[ignore = "对真实服务器做写操作"]
async fn main_workflow() {
    let ctx = Ctx::prepare().await;
    step_01_query_root_self(&ctx).await;
    step_02_query_root_members(&ctx).await;
    // ...
}
```

### 4.2 三层结构与依赖链

```text
main_workflow          （编排：只调用，不含逻辑）
  ├─ 阶段一函数 phase_01_setup_tree
  │      └─ step_01_…  step_02_…  … 每个步骤一个函数
  ├─ 阶段二函数 phase_02_copy
  ├─ 阶段三函数 phase_03_move
  └─ 阶段四函数 phase_04_delete
negative_cases         （编排：10 个负向用例各自一个函数）
cleanup_and_baseline_compare
```

**依赖链**：正向链路的所有步骤共享同一个命名空间，因此它们必须在**同一个测试进程里按顺序
执行**。这意味着运行命令**必须带 `--test-threads=1`**——否则同进程内的步骤会被并行打乱，
后一步看不到前一步创建的资源。

这是明确的取舍：把步骤拆成独立函数是为了**可读、可单步调试**；共享命名空间是为了**不把
建树动作重复十几遍、不给服务器制造数倍请求量**。两者并存的方式就是「单进程 + 顺序执行 +
每步一个函数」。

### 4.3 各阶段包含的步骤

| 阶段 | 步骤 | 说明 |
|---|---|---|
| 阶段零 | 准备：构造 Auth、清残留、记录基线快照 | 步骤 1/2 的基线在此采集 |
| 阶段一 | 步骤 1–26 | 查询根 → 建 01/02/03 → 建四个文件 → 逐项核对 |
| 阶段二 | 步骤 27–32 | 复制 `文件C` 到 01 与 03 → 核对副本与原源仍在 |
| 阶段三 | 步骤 33–46 | 建 04 → 移动 `文件D` 到 04 → 核对源消失 → 删 02/03 |
| 阶段四 | 步骤 47–49 | 终态逐项核对 → 清理 → 根目录与基线比对 |

### 4.4 为什么不做成 49 个 `#[tokio::test]`

若每个步骤都是一个独立 `#[tokio::test]`，每个测试都得自己把整套目录树重建一遍，
等于把「建 01、建 02、建 03、传四个文件」重复十几遍，对真实服务器造成数倍请求量，
而且每个测试都要独立命名空间才能避免互相干扰。

因此采用「**一个 `#[tokio::test]` + 每步一个 `async fn`**」：可读性与可单步调试性来自
函数粒度，请求量来自共享命名空间。失败时输出里会打印正要执行的步骤名，定位仍然精确到步。

---

## 五、查询动作全集

查询不是开头的单独一步，而是**贯穿全程的核对手段**，在每个阶段前后各跑相关的那几项。
一共四类手段、十七种查询：

### 5.1 结构查询（PROPFIND）

| 编号 | 查询 | 手段 | 期望 |
|---|---|---|---|
| Q1 | 根目录成员 | PROPFIND `Depth: 1` `allprop` | 207，能列出既有成员 |
| Q2 | 根目录自身 | PROPFIND `Depth: 0` | 只返回自身一项 |
| Q3 | 只问属性名 | PROPFIND `propname` | 属性名列表非空 |
| Q4 | 指定属性 | PROPFIND `props([Resourcetype, Getcontenttype, Getetag, Getlastmodified])` | 四项都能取到 |
| Q5 | 单资源属性 | PROPFIND `Depth: 0` 指定属性 | `resourcetype` / `content_length` / `getetag` 符合预期 |
| Q6 | 集合的直接子项 | PROPFIND 集合 `Depth: 1` | 子项集合**精确相等**（比较集合，不比较顺序） |
| Q7 | 集合递归 | PROPFIND 集合 `Depth: infinity` | 含全部后代；**本机服务器会 403，见第九章** |
| Q8 | 不存在的路径 | PROPFIND 不存在路径 | 非 207 → `send_and_deserialize` 报错，据此判定「查不到」 |

### 5.2 元数据查询（HEAD）

| 编号 | 查询 | 期望 |
|---|---|---|
| Q9 | 文件存在性 | 200；不存在则 404 |
| Q10 | 长度与类型 | `Content-Length` 等于写入字节数 |
| Q11 | 条件 HEAD | 带正确 `if-none-match` → 304 |

### 5.3 内容查询（GET）

| 编号 | 查询 | 期望 |
|---|---|---|
| Q12 | 单文件逐字节比对 | 与 PUT 进去的字节完全一致 |
| Q13 | 集合 GET | 记录服务端实际返回，**不假设** |
| Q14 | 条件 GET | 带正确 `If-None-Match` → 304 |

### 5.4 能力查询（OPTIONS）

| 编号 | 查询 | 期望 |
|---|---|---|
| Q15 | 根能力 | `dav_levels` 含 `1`；`allowed_methods` 含本次要用的方法 |
| Q16 | 单资源能力 | 200，返回自身能力头 |
| Q17 | 能力**驱动**流程 | 服务端不支持 COPY/MOVE 就断言失败并说明原因，而不是盲发请求 |

Q17 是这套测试与"照着步骤发请求"的区别：能力声明是前置条件，不满足就明确报错。

---

## 六、完整工作流步骤

「前置查询」在动作之前跑，「后置核对」在动作之后跑。核对一律以服务端记录为准。

| 步骤 | 动作 | 用到的能力 | 立即核对 |
|---|---|---|---|
| 1 | 查询当前目录自身 | PROPFIND `Depth:0` `allprop` | 207，只有自身一项；**记录根成员基线快照** |
| 2 | 查询当前目录成员 | PROPFIND `Depth:1` `allprop` | 207，列出既有成员 |
| 3 | 只问属性名 | PROPFIND `propname` | 属性名列表非空 |
| 4 | 指定属性查询 | PROPFIND `props([Resourcetype, Getcontenttype, Getetag, Getlastmodified])` | 四项都能取到 |
| 5 | 问服务端能力 | OPTIONS `send_capabilities()` | Q15；缺少后续要用的方法即失败 |
| 6 | 确认 `测试目录01` 不存在 | PROPFIND 该路径 `Depth:0` | 非 207 → 判定查不到 |
| 7 | 创建 `测试目录01` | MKCOL | 201 |
| 8 | 核对 01 | PROPFIND `Depth:0` 指定属性 | `resourcetype` 是 collection |
| 9 | 核对 01 内容 | PROPFIND `Depth:1` | 子项为空 |
| 10 | 确认 02 / 03 不存在 | PROPFIND ×2 | 都查不到 |
| 11 | 在 01 内创建 `测试目录02` | MKCOL | 201 |
| 12 | 在 01 内创建 `测试目录03` | MKCOL | 201 |
| 13 | 核对 01 | PROPFIND `Depth:1` | 子项**恰好** `[02, 03]` |
| 14 | 核对 02、03 自身 | PROPFIND ×2 `Depth:1` | 都是空集合 |
| 15 | 确认 `文件A` 不存在 | PROPFIND | 查不到 |
| 16 | 在 01 内建 `文件A`（内存源） | PUT `U8Bytes` | 2xx |
| 17 | 在 01 内建 `文件B`（文件句柄源） | PUT `FileHandle` | 2xx |
| 18 | 核对 01 的两个文件 | HEAD ×2 | 200；`Content-Length` 等于写入字节数 |
| 19 | 读回内容 | GET ×2 | 与写入字节**逐字节一致** |
| 20 | 条件 HEAD | HEAD `if-none-match` | 304 |
| 21 | 条件 GET | GET `If-None-Match` | 304 |
| 22 | 核对 01 结构 | PROPFIND `Depth:1` | 子项 = `[02, 03, 文件A, 文件B]` |
| 23 | 在 02 内建 `文件C` | PUT | 2xx |
| 24 | 在 03 内建 `文件D` | PUT | 2xx |
| 25 | 核对 02、03 | PROPFIND ×2 `Depth:1` | 02 子项=`[文件C]`；03 子项=`[文件D]` |
| 26 | 读回 C、D | GET ×2 | 逐字节一致 |
| 27 | 确认两个复制目标都不存在 | PROPFIND ×2 | 查不到 |
| 28 | 把 C 复制到 01 | COPY `Depth::Zero` + `Overwrite::False` | 201（源=`02/文件C`，目标=`01/文件C`） |
| 29 | 把 C 复制到 03 | COPY `Depth::Zero` + `Overwrite::False` | 201（目标=`03/文件C`） |
| 30 | 核对两个副本 | HEAD + GET ×2 | 存在；长度与内容与 C 一致 |
| 31 | 确认 COPY 不删源 | HEAD 源 | 200，源仍在 |
| 32 | 核对 01、03 结构 | PROPFIND ×2 `Depth:1` | 01 多了 `文件C`；03 多了 `文件C` |
| 33 | 确认 `测试目录04` 不存在 | PROPFIND | 查不到 |
| 34 | 创建 `测试目录04` | MKCOL | 201 |
| 35 | 核对 04 | PROPFIND `Depth:1` | 空集合 |
| 36 | 核对 01 结构 | PROPFIND `Depth:1` | 子项 = `[02, 03, 04, 文件A, 文件B, 文件C]` |
| 37 | 核对移动前状态 | HEAD 源 `03/文件D`、HEAD 目标 `04/文件D` | 源 200；目标 404 |
| 38 | 把 03 的文件移到 04 | MOVE `move_from_path` + `Overwrite::False` | 201 |
| 39 | 核对移动结果 | HEAD + GET 目标 `04/文件D` | 200；内容与 D 一致 |
| 40 | 核对源已消失 | HEAD + PROPFIND 源 `03/文件D` | 都查不到 |
| 41 | 核对 03、04 结构 | PROPFIND ×2 `Depth:1` | 03 子项=`[文件C]`；04 子项=`[文件D]` |
| 42 | 删除前记录 02、03 形态 | PROPFIND ×2 `Depth:1` | 02 非空（含 C）；03 非空（含 C）——留作对照 |
| 43 | 删除 `测试目录02` | DELETE `Depth::Infinity` | 204 |
| 44 | 删除 `测试目录03` | DELETE `Depth::Infinity` | 204 |
| 45 | 核对已删除 | PROPFIND ×2 | 都查不到 |
| 46 | 核对 01 结构 | PROPFIND `Depth:1` | 子项 = `[04, 文件A, 文件B, 文件C]` |
| 47 | 终态逐项核对 | PROPFIND `Depth:0` 指定属性 + GET ×4 | 01 内每个成员的属性与内容都对；04 内确实还是 `文件D` |
| 48 | 清理：删除 `测试目录01` | DELETE `Depth::Infinity` | 204 |
| 49 | 核对根恢复 | PROPFIND `Depth:1` | 与步骤 1/2 的**基线快照逐项一致** |

步骤 43/44 的删除对象形态是有意安排的：删除前 02 与 03 **都非空**（各含一个 `文件C`），
因此这两步验证的是「非空集合的递归删除」。而 `测试目录03` 在 MOVE 之后、收到 `文件C`
之前的短暂窗口里是空集合 —— 空集合删除由 N10 单独覆盖。两者合起来满足「删除空文件夹与
非空文件夹都要测」的要求。

---

## 七、负向用例

与正向步骤对称，穿插在执行过程中。负向用例的价值不低于正向：它验证失败路径**没有副作用**。

| 编号 | 场景 | 期望 |
|---|---|---|
| N1 | MKCOL 已存在的 `测试目录02` | 非成功（通常 405），且 02 内容**未被破坏** |
| N2 | MKCOL 直接建 `01/新目录/子目录`（父不存在） | 非成功（通常 409），父路径未被创建 |
| N3 | COPY 不存在的源 | 非成功（通常 404），且**目标未被创建** |
| N4 | MOVE 不存在的源 | 非成功，且目标未被创建 |
| N5 | COPY 到已存在目标 + `Overwrite::False` | 412，且既有目标内容**逐字节未变** |
| N6 | MOVE 到已存在目标 + `Overwrite::False` | 非成功，且**源仍在、目标未变** |
| N7 | DELETE 不存在的路径 | 非成功（通常 404） |
| N8 | DELETE 非空集合 + `Depth::Zero` | 服务端拒绝（本机 400），且**集合内容未丢** |
| N9 | MOVE 集合到自身子路径 | 非成功（通常 403） |
| N10 | DELETE 空集合 + `Depth::Infinity` | 成功 —— 与 N8 构成空 / 非空对照 |

N5、N6、N8 都必须核对**副作用为零**，这是最容易出错也最容易造成真实损失的地方。

---

## 八、实现要点

### 8.1 操作助手 `DavTree`

封装真实服务器操作，让用例读起来接近自然语言：

| 助手 | 作用 |
|---|---|
| `auth()` | 读取环境变量并构造 `WebdavAuth` |
| `namespace()` | 生成本次运行的唯一命名空间前缀 |
| `cleanup_prefix(&auth, prefix)` | 列出根目录，删除所有匹配前缀的顶层项（含上次残留） |
| `children(&auth, path)` | PROPFIND `Depth:1`，返回子项路径集合 |
| `exists(&auth, path)` | PROPFIND `Depth:0`；非 207 视为不存在 |
| `is_collection(&auth, path)` | 读 `resourcetype` 判断是不是集合 |
| `read(&auth, path)` | GET 并返回字节 |
| `length(&auth, path)` | HEAD 取 `Content-Length` |
| `put_bytes(&auth, path, bytes)` | PUT 内存源 |
| `put_file(&auth, path)` | 用临时文件走 `FileHandle` 源 |

`children` 返回**集合**而不是 `Vec`（或返回 `Vec` 但在断言前排序），因为服务端返回顺序
不保证。

### 8.2 清理责任

- **每个测试函数开头先清一次**：删掉所有 `__webdav_core_workflow_` 前缀的顶层残留（包括
  上一次失败留下的）。这让整套测试幂等，不会累积垃圾。
- **结尾再清一次**并核对根目录。
- 清理失败只把残留名字打印到输出，**不掩盖断言失败**，也不让清理异常吞掉真实结果。
- 清理与基线核对也是**各自独立的步骤函数**（`step_48_…` / `step_49_…`），不塞进编排函数里。

### 8.3 每个步骤函数的写法
每个步骤函数遵循同一形状，便于逐个阅读与单步调试：

- 函数名带步骤号与动作，例如 `step_28_copy_file_c_to_dir01`。
- 进入时打印一行步骤名（`--nocapture` 下形成完整轨迹）。
- 只做**一件事**：一个动作，或一次核对。动作与核对**分开成两个函数**，不合并。
- 失败信息里带上服务端实际返回（状态码、相关响应头或响应体片段）。

```rust
/// 步骤 28：把 02 里的 文件C 复制到 01。
async fn step_28_copy_file_c_to_dir01(ctx: &Ctx) {
    step("28. 把 文件C 从 02 复制到 01");
    let response = ctx
        .auth()
        .copy()
        .source_path(&ctx.path("测试目录02/文件C"))
        .expect("源路径应合法")
        .target_path(&ctx.path("测试目录01/文件C"))
        .expect("目标路径应合法")
        .depth(CopyDepth::Zero)
        .overwrite(Overwrite::False)
        .send()
        .await
        .expect("COPY 应发送成功");
    assert_eq!(response.status(), 201, "复制到不存在的位置应返回 201");
}

/// 步骤 30a：核对 01 里的副本与源内容一致。
async fn step_30a_verify_copy_in_dir01(ctx: &Ctx) {
    step("30a. 核对 01 里的副本内容");
    let copied = ctx.read(&ctx.path("测试目录01/文件C")).await;
    let original = ctx.read(&ctx.path("测试目录02/文件C")).await;
    assert_eq!(copied, original, "副本内容应与源逐字节一致");
}
```

### 8.4 断言语义

- 集合比较用**集合语义**（排序后比较，或直接比较 `BTreeSet`），不比较服务端返回顺序。
- href 比较前统一去掉尾部斜杠差异与百分号编码差异。
- 状态码断言用集合（如 `assert!(status.is_success())`）而非逐码断言，除非该步的码是协议
  确定的（例如 `Overwrite::False` 撞已存在目标应是 412）。
- 失败信息里带上**服务端实际返回**（状态码、`DAV`/`Allow` 头、或响应体片段），便于定位。

---

## 九、验收服务器已知的专属行为

本项目当前的验收服务器（Teracloud，Apache `mod_dav`）有几处与 RFC 默认值不同，实测如下。
测试必须按此编写，并在注释里注明，避免后人误判为库的缺陷：

| 请求 | 结果 | 影响 |
|---|---|---|
| `PROPFIND` 根目录 `Depth: infinity` | **403** | 只接受 `0` 与 `1`。`PropFindBuilder` 默认 Depth 是 infinity，对根目录用它会失败，需显式设 Depth |
| `DELETE` 集合 `Depth: 0` | **400** | 集合删除必须用 `Depth: infinity` |
| `DELETE` 无 `Depth` 头 | 204 | 与 `infinity` 等价 |
| `DELETE` 非空集合 `Depth: infinity` | 204 | 递归删除可用 |

因此本方案里：

- 核对「某路径已不存在」一律用 `Depth: 0` 的单层 PROPFIND。
- 清理与删除集合一律用 `DeleteDepth::Infinity`。
- 不使用 `Depth: infinity` 的 PROPFIND（Q7 只作为**记录行为**，断言「要么 207 要么 403」）。

### 9.1 已知的不稳定因素

经代理访问真实服务器时，偶发**传输层**失败（`tls handshake eof`），与请求内容无关，
单独重跑即通过。这类失败应如实报出，不掩盖、也不通过放宽断言语义来"修复"。

---

## 十、执行方式

### 10.1 凭据与代理

`WEBDAV_PASSWORD` 只存在于 Windows 的 User 作用域而没有进入父进程环境，子进程读不到；
且本机出网经本地代理，而 `reqwest` 默认不读系统代理。因此运行前需显式注入：

```powershell
$env:WEBDAV_PASSWORD = [Environment]::GetEnvironmentVariable('WEBDAV_PASSWORD','User')
$env:WEBDAV_URL      = [Environment]::GetEnvironmentVariable('WEBDAV_URL','User')
$env:WEBDAV_ACCOUNT  = [Environment]::GetEnvironmentVariable('WEBDAV_ACCOUNT','User')
$env:HTTPS_PROXY     = 'http://127.0.0.1:17890/'
$env:HTTP_PROXY      = 'http://127.0.0.1:17890/'
```

### 10.2 运行

```powershell
cargo test --features network-test --test workflow -- --ignored --nocapture --test-threads=1
```

- `--nocapture` 是为了看到每一步的打印轨迹，便于人工核对服务器上真实发生了什么，
  也让失败时能立刻看到停在哪个步骤。
- `--test-threads=1` **是必须的，不是可选项**：正向链路的 49 个步骤共享同一个命名空间，
  并行执行会打乱依赖顺序，后一步看不到前一步创建的资源（见 4.2）。
- **一轮只跑一次。** 不为了"证明稳定"而重复批量运行。

### 10.3 跑完必须做的核对

1. 把服务器根目录清单贴给项目所有者核对，确认既有内容一项未少。
2. 确认没有 `__webdav_core_` 前缀的残留；若有，按残留名人工清理。

---

## 十一、验收标准

1. 正向链路的全部步骤函数与 10 个负向用例函数全部通过，且**协议语义层面零失败**
   （传输层瞬时失败单独报告）。
2. 步骤 49 的根目录与步骤 1/2 的基线快照逐项一致。
3. 服务器既有内容零改动、零残留。
4. 全部 10 个负向用例的**副作用为零**均已核对（N5/N6/N8 的既有内容逐字节未变）。
5. 输出中不出现账号、密码或 `Authorization`。
6. 每个断言失败时都能从输出看到服务端实际返回值。

### 11.1 失败时怎么办

如果某一步在真实服务器上失败，**先判断是服务器行为差异还是库的缺陷**：

- 若服务端返回了协议允许范围内的另一种结果（例如用 405 而非 409），按第九章的方式记录为
  服务器专属行为，调整该步期望，并在注释里注明。
- 若服务端返回的结果说明**库发出的请求不符合协议**（例如 `Destination` 没编码、少发了
  `Depth`），那是库的缺陷，必须改库代码，不允许改断言绕过。
- 若属于传输层瞬时失败，重跑一次确认；不通过放宽断言语义来"修复"。

---

## 十二、维护约定

- 新增 WebDAV 能力时，在步骤表里补一段，并同步更新第五章的查询矩阵。
- 不要为了让测试变绿而放宽断言；第九章记录了合法的服务器差异，其他差异要么是库的缺陷，
  要么是方案需要修订。
- 这套测试**不计入日常覆盖率门槛**（按 `docs/测试/测试规范.md`，真实网络测试不参与覆盖率
  结论），它的价值是行为验证，而非覆盖率贡献。
